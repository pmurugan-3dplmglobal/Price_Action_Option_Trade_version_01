Here's a complete before→after reference of every change I've applied so far (these edits were made in the previous turns; I'm now in read-only/plan mode, so no further edits until you approve).
1. input/program_config.json — rewritten
Before: hardcoded separate _backtest per engine, no credentials, lookbacks 100/200, daily 500, no api keys.
After:
{
  "api_key": "o8nnw6kxykvrsrhg",
  "api_secret": "9g7d5kktr38d7yvq11njsm4upz8kc6s1",
  "daily":  { "timeframe": "day",     "lookback_days": 500 },
  "_backtest": false,
  "index":   { "timeframe": "3minute", "lookback_days": 100, "scan_interval": 15, "risk_percent": 1,  "capital": 100000 },
  "nifty50": { "timeframe": "15minute","lookback_days": 200, "scan_interval": 30, "risk_percent": 10, "capital": 100000 }
}
(lookbacks now match Kite's real per-timeframe caps — index 3min→100, nifty50 15min→200, daily day→2000.)
2. app.py — credentials moved out of source + anchor double-trigger fix
- Removed API_KEY/API_SECRET constants; added get_kite_credentials() reading from config.
- get_login_url() and exchange_request_token() now use get_kite_credentials().
- api_anchor_scan() no longer writes ANCHOR_SCAN_REQUEST_FILE (the running engine also polled it → would run the anchor scan twice). It now relies only on the --anchor-only subprocess.
3. bull_index_trade_engine.py — cycle batching
- execute_scan_cycle() now stages each found trade to temp storage (trade_db.stage_cycle_trade) + logs it, and skips already-executed patterns (trade_db.is_pattern_executed). It no longer creates an active trade immediately. Returns the staged list.
- Added execute_index_entry(kite, pos) (places BUY market MIS order live / logs DRY in backtest).
- Added execute_best_trade(kite, staged): if >2 staged trades, executes the one with max (T3−entry), records the pattern as executed.
- background_loop: after each cycle, if >2 staged → execute best; else logs "need >2"; then trade_db.clear_cycle_trades("index").
- Limit map corrected to Kite values (3minute:100, 15minute:200, 60minute:400, day:2000, …).
4. bull_nifty50_scanner_executor.py — cycle batching
- execute_scan_cycle() now stages trades (dedup + stage_cycle_trade) and returns the staged list instead of executing the best immediately.
- Added execute_best_trade(kite, staged): if >2 staged, executes max-profit trade (BUY LIMIT NRML live / BACKTEST_BEST log), records executed pattern.
- background_loop: stages → if >2 execute best else skip → clear_cycle_trades("nifty50").
- Limit map corrected to Kite values.
5. bull_nifty50_daily_scanner_export.py
- main() now handles --anchor-only (previously ignored → clicking "Anchor" for daily did a full scan).
- load_program_config() reads timeframe (so daily default day is configurable).
- run_scan lookback cap 500 → 2000 (Kite day limit).
6. trade_db.py — new helpers
Added stage_cycle_trade / get_cycle_trades / clear_cycle_trades (temp store output/monitor/cycle_trades.json) and is_pattern_executed / record_executed_pattern (registry output/monitor/executed_patterns.json).
7. Read_Me_Notes/DOCUMENTATION.md
Updated: architecture diagram + file map to bull_* names + app.py; config schema to single timeframe + global _backtest; correct Kite lookback-limit table; config JSON example (with api_key/api_secret); added notes on cycle-batching execution model and temp/executed stores.
STILL PENDING (not yet done)
- Read_Me_Notes/CLOUD_DEPLOYMENT_GUIDE.md still references old names (index_trade_engine.py, nifty50_scanner_executor.py, trading_dashboard.py) at lines 137/142/147/183 — needs the same rename fixes.
- Delete stale __pycache__ (old-name .pyc + phantom trading_dashboard.pyc).
- Final py_compile/py_compile smoke check across all modules.
Want me to resume and finish the two pending items, or adjust any of the changes above first?